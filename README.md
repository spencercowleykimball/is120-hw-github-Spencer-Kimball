This REPO is for Spencer Kimball's HW 3.
It contains no secret files, because those are secret.
It contains a .gitignore which ignores no secret files, because those are secret.
The .gitignore also ignores a junk folder which literally has the most basic html script in there.
I also added a index.html which was my file from HW 2 which is an extremely basic website that hypes up the new Selkirk Boomstik pickleball paddle.